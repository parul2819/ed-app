.env
venv/
.venv/
__pycache__/
*.pyc
.pytest_cache/
.DS_Store
debug_api_test.py

# Raw source images for the background-image build pipeline (see kids-friendly-ui
# skill section 6). The optimized output lives in frontend/public/backgrounds/ and
# is generated with `npm run build:backgrounds` — that's what's committed.
background images/

# Output of scripts/audit_questions.py -- regenerate, don't commit.
audit_report.txt

# Local design-reference material (mood-board screenshots, purchased Etsy PDF)
# used while styling the button "wool" theme -- not app assets, not imported
# by any code.
/icons/
/ETSY - animals Cartoons A-Z.pdf
